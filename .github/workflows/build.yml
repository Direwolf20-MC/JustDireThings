name: Build

on:
  push:
    branches:
      - main
  pull_request:
      branches:
        - main

jobs:
  build:
    if: |
      !contains(github.event.head_commit.message, '[ciskip]')
    uses: ./.github/workflows/base.yml
    with:
      gradle-tasks: 'build'
